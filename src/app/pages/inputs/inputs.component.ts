import { Component } from "@angular/core";
import { NOMEM } from "dns";

@Component({
    selector: 'app-input',
    templateUrl:"./inputs.componente.html"
})


export class InputsComponent {
    
    darConsole(nome: string, genero:string, idade:number){
        this.nome
    }
}